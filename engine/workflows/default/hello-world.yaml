workflow:
  name: hello-world
  trigger:
    type: api

  steps:
    - id: run_echo
      type: action
      action: command_module.Command.run
      input:
        command: "echo Hello from SeyoAWE"
      register_output: echo_result
